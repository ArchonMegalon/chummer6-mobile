# Worklist Queue

Purpose: queue actionable items and run them in order without losing momentum.

## Status Keys
- `queued`
- `in_progress`
- `blocked`
- `done`

## Queue
| ID | Status | Priority | Task | Owner | Notes |
|---|---|---|---|---|---|
| WL-001 | done | P1 | Establish the `chummer-play` solution skeleton and package consumption boundaries so the repo consumes `Chummer.Engine.Contracts`, `Chummer.Play.Contracts`, and `Chummer.Ui.Kit` without raw project or source references. | agent | Completed 2026-03-09 and canonicalized 2026-03-09: added `Chummer.Play.slnx`, shared build props, central package versions, core/components/player/gm/web project skeletons, and verify guardrails that fail on copied contract trees or cross-repo project references. |
| WL-002 | done | P1 | Extract the current session-web/mobile play host seam into `Chummer.Play.Web` and define the repo structure for player shell, GM shell, components, and offline core services. | agent | Completed 2026-03-09 as the first extraction pass: `Chummer.Play.Web` now owns the session API client, coach client, event-log store, offline cache, and shell probe seams plus a migration map and separate player/GM/core/component projects. The next extraction work is to replace placeholders with moved implementation from `Chummer.Session.Web` without bringing workbench dependencies across. |
| WL-008 | done | P1 | Replace the current placeholder browser session clients and offline services with concrete code moved from `Chummer.Session.Web` while preserving package-only dependencies. | agent | Completed 2026-03-10: scaffold bootstrap/client/storage seams now resolve through concrete `/api/play/*` handlers, `HttpClient` sync flows, and browser-state-backed event ledger/cache services. |
| WL-003 | done | P1 | Write the initial `chummer-play.design.v1.md` plus dependency and ownership rules for offline event logs, runtime bundle consumption, role shells, and play-safe shared UI usage. | agent | Completed 2026-03-09: added initial design doc plus dedicated sync/offline notes documenting local-first storage and play-only ownership boundaries. |
| WL-004 | done | P1 | Align the play client contract surface around canonical engine session contracts plus a narrow `Chummer.Play.Contracts` bootstrap/projection/sync API family. | agent | Completed 2026-03-10: play API contracts now share a canonical `EngineSessionEnvelope`/`EngineSessionCursor` across bootstrap/projection/reconnect/sync shapes, and both `Program.cs` and `BrowserSessionApiClient` use that envelope-first surface instead of repeated ad hoc session fields. |
| WL-005 | in_progress | P1 | Build the local-first play runtime seam for event-log persistence, runtime bundle caching, offline queueing, and sync/replay abstractions. | agent | Umbrella storage/sync epic only. Land browser persistence through WL-011, then extend into runtime cache and replay hardening without absolute tracker overwrite paths. Incremental hardening landed 2026-03-10: sync now acknowledges and trims accepted pending events while preserving ledger sync provenance metadata (`LastSyncedAtUtc`, `LastAcceptedEventCount`). Follow-up hardening landed 2026-03-10: `/api/play/sync` no longer re-appends request pending events before acknowledgement, preventing duplicate pending-event residue during local replay. Additional hardening landed 2026-03-10: sync acknowledgement now trims only contiguous matching pending-event prefixes, preventing mismatched payloads from dropping unrelated local pending events. Runtime seam hardening landed 2026-03-10: offline queue enqueue and sync/replay acknowledgement logic now run through `BrowserSessionOfflineQueueService` instead of inline route handlers, preserving replay behavior behind a dedicated local-first abstraction boundary. API hardening landed 2026-03-10: `/api/play/sync` and `/api/play/quick-action` now reject malformed cursor/session payloads and blank pending-event entries with deterministic `400` responses to protect offline replay seams from null-driven runtime faults. Additional stale-lineage hardening landed 2026-03-10: reconnect now validates malformed cursor/session payloads before dereference, and stale quick-action/sync responses prefer stored checkpoint or ledger lineage when checkpoint state is missing, preserving deterministic stale recovery against browser-ledger truth. Follow-up stale replay hardening landed 2026-03-10: stale quick-action/sync responses now rebuild projections from stored ledger pending events and `LastKnownSequence`, and when a checkpoint exists it is advanced to ledger sequence ownership instead of regressing replay state. Queue-seam hardening landed 2026-03-10: `BrowserSessionOfflineQueueService` now enforces stored checkpoint+ledger lineage alignment before enqueue or sync acknowledgement, so stale direct queue callers cannot mutate offline replay state outside route-layer stale protection. Additional resume/bootstrap lineage hardening landed 2026-03-10: bootstrap and resume now normalize to stored ledger/checkpoint lineage before mutating browser state, stale responses rebuild aligned checkpoints instead of returning mismatched lineage, and regression coverage now proves stale retry recovery plus stale-bootstrap/resume preservation. Abstraction seam hardening landed 2026-03-10: route handlers and play web runtime now depend on `IPlayEventLogStore`, `IPlayOfflineCacheService`, and `IPlayOfflineQueueService` contracts, keeping event-log persistence, runtime bundle cache, and offline sync/replay queue ownership behind explicit local-first interfaces. Endpoint stale-safety hardening landed 2026-03-11 from PR feedback: `/api/play/reconnect` now performs stored-lineage stale guards before any ledger mutation, and quick-action/sync now convert queue-level lineage drift exceptions into stale responses instead of surfacing `500`s. Client stale-reconnect hardening landed 2026-03-11 from PR feedback: `BrowserSessionApiClient.ReconnectAsync` now handles `409` conflict payloads as typed `PlayReconnectStaleException` (projection+checkpoint preserved), and regression coverage now verifies route-level stale bootstrap normalization plus typed stale reconnect-client behavior. Queue-input hardening landed 2026-03-11: `BrowserSessionOfflineQueueService.SyncReplayAsync` now rejects null/blank pending-event payloads for direct callers, and regression coverage asserts seam-level malformed-payload rejection outside route-only validation. Additional seam hardening landed 2026-03-11: `BrowserSessionEventLogStore.AppendPendingEventsAsync` now rejects blank lineage fields, empty/blank pending-event payloads, and negative sequence ownership for direct callers, while queue cursor validation now rejects malformed session envelopes before offline mutation. Monotonic-sequence seam hardening landed 2026-03-11: `BrowserSessionEventLogStore.AppendPendingEventsAsync` now rejects sequence regressions below stored ledger ownership for direct callers, and regression coverage asserts event-log sequence regression is blocked instead of silently mutating replay order. Offline-cache seam hardening landed 2026-03-11: `BrowserSessionOfflineCacheService` now rejects malformed checkpoint/runtime-bundle metadata for direct callers (blank lineage fields and negative checkpoint sequence ownership), and regression coverage now asserts these direct-caller guards. Corruption-read hardening landed 2026-03-11: cache/event-log read paths now drop malformed persisted checkpoint/runtime-bundle/ledger records before reuse, preserving local-first replay and stale-safety behavior when browser storage is tampered or stale-corrupt. |
| WL-006 | done | P2 | Split player and GM role shells with capability gating, tactical Spider cards, coach hints, and stale-protected quick actions for mobile and tablet play. | agent | Completed 2026-03-10: bootstrap is role-gated, player flows no longer receive GM shell metadata/actions, tactical Spider cards and coach hints are role-capability-scoped, and stale-protected `/api/play/quick-action` handling now persists through the local event ledger/checkpoint seam. |
| WL-007 | done | P2 | Add installable PWA hardening for service-worker cache policy, storage quota/backpressure, and media-cache lifecycle for the play shell. | agent | Completed 2026-03-10: service worker now partitions shell/API/media caches, applies bounded media TTL+LRU-style pruning, and performs quota-failure backpressure cleanup before retry to preserve installable play-shell integrity under storage pressure. |
| WL-009 | done | P1 | Replace the scaffold `/api/play/bootstrap` endpoint in `Chummer.Play.Web` with a canonical bootstrap handler backed by `Chummer.Play.Contracts` response types and explicit role-shell bootstrap descriptors. | agent | Completed 2026-03-10: bootstrap now emits a role-aware response with active shell selection, projection state, and shell probe metadata instead of static notes. |
| WL-010 | done | P1 | Replace `BrowserSessionApiClient` placeholder string projections with executable browser session fetch/reconnect/sync flows against the dedicated play API seam. | agent | Completed 2026-03-10: client now drives bootstrap, projection, reconnect, and stale-aware sync through concrete JSON HTTP requests. |
| WL-011 | done | P1 | Replace `BrowserSessionEventLogStore` in-memory placeholder storage with browser-backed event-log persistence and replay-safe ledger ownership. | agent | Completed 2026-03-10: ledger state now persists through the browser storage seam by session key with replay metadata, sequence tracking, and provenance fields. |
| WL-012 | done | P1 | Materialize the dedicated `/api/play/*` surface beyond bootstrap with explicit projection, reconnect, and sync route ownership in `Chummer.Play.Web`. | agent | Completed 2026-03-10: `Chummer.Play.Web` now owns explicit projection, reconnect, and sync endpoints with stale-revision rejection. |
| WL-013 | done | P1 | Materialize browser offline cache ownership for runtime bundles, sync checkpoints, and replay metadata alongside the event ledger seam. | agent | Completed 2026-03-10: offline cache service now persists runtime bundle lineage and sync checkpoints through the browser-state seam. |
| WL-014 | done | P2 | Harden the installable play shell for PWA usage with manifest, service worker cache policy, quota/backpressure handling, and deep-link resume ownership. | agent | Completed 2026-03-10: added installable manifest/static shell assets, service-worker cache policy with API/network-first and navigation fallback handling, bounded runtime-bundle quota/backpressure eviction snapshots, and dedicated `/api/play/resume/{sessionId}` + `/play/{sessionId}` deep-link ownership. |
| WL-015 | done | P1 | Publish runnable backlog and enforce package-only dependency boundaries for `Chummer.Engine.Contracts`, `Chummer.Play.Contracts`, and `Chummer.Ui.Kit` directly in repo verification. | agent | Completed 2026-03-10: hardened `scripts/ai/verify.sh` to reject unsupported `Chummer.*` package references and reject direct literal references to the three approved packages so package usage stays centralized through shared package-id properties. |
| WL-016 | done | P1 | Normalize contract package naming between play repo docs and package props by renaming the engine package-id/version property surface to explicit `ChummerEngineContracts*` semantics and documenting the migration seam. | agent | Completed 2026-03-10: renamed engine package-id/property usage to `ChummerEngineContracts*`, split central package versions by contract family (`ChummerEngineContractsPackageVersion`, `ChummerPlayContractsPackageVersion`, `ChummerUiKitPackageVersion`), and hardened `scripts/ai/verify.sh` to reject legacy `ChummerContracts*` naming so docs and package props stay consistent. |
| WL-017 | done | P1 | Cut over play dependency consumption from local package stubs to published `Chummer.Play.Contracts` and `Chummer.Ui.Kit` package feeds with restore-time compatibility checks. | agent | Completed 2026-03-10: published-feed compatibility checks are executable through `scripts/ai/with-package-plane.sh` + `CHUMMER_PUBLISHED_FEED_SOURCES` (with optional `CHUMMER_PUBLISHED_*_VERSION` overrides), and `scripts/ai/verify.sh` now validates restore/build compatibility across core/components/player/gm/web/regression projects on both explicit published feeds and repo-local published-feed smoke fallback (packed stub artifacts), preserving offline executability without source-copy contracts. Traceability: feedback candidates 8675 and 48737. |
| WL-018 | done | P1 | Add scaffold-regression guards so bootstrap, browser session client, and browser event-log persistence cannot silently fall back to placeholder seams. | agent | Completed 2026-03-10 for stale queue/finding closure: `scripts/ai/verify.sh` now enforces concrete `/api/play/*` route ownership, executable HTTP sync calls, browser-backed event-log persistence seams, and fails on explicit scaffold/placeholder TODO markers in those files. Traceability: feedback `2026-03-09-change-plan-mobile.md`, audit task 2653 queue overlay follow-up, and `.codex-studio/published/QUEUE.generated.yaml` scaffold-placeholder mapping closure lines dated 2026-03-10. |

## Mobile Milestone Registry

Date basis: last updated 2026-03-10 (UTC).

| Milestone | Scope | Worklist Coverage | Status | ETA | Completion Truth |
|---|---|---|---|---|---|
| M1 | Contract canon and package-only dependencies | WL-001, WL-015 | done | 2026-03-10 (actual) | Complete via package-only build guardrails, package-stub verification, and explicit enforcement that only approved package-id properties reference the shared package plane. |
| M2 | Session-web/mobile host seam extraction | WL-002 | done | 2026-03-09 (actual) | Complete via `Chummer.Play.Web` host seam ownership extraction. |
| M3 | Replace scaffold bootstrap/client/event-log seams with executable runtime code | WL-008, WL-009, WL-010, WL-011 | done | 2026-03-10 (actual) | Complete via concrete `/api/play/*` handlers, HTTP sync flows, and browser-backed ledger persistence. |
| M4 | Dedicated `/api/play/*` ownership and contract alignment | WL-004, WL-012, WL-016, WL-017 | done | 2026-03-10 (actual) | Complete: route ownership (WL-012), canonical session-envelope alignment (WL-004), package naming normalization (WL-016), and published-feed package-plane cutover with restore-time compatibility checks (WL-017) are all implementation-backed. |
| M5 | Player and GM role shell separation and gating | WL-006 | done | 2026-03-10 (actual) | Complete via role-gated shell bootstrap payloads and capability-scoped quick actions that block cross-role exposure. |
| M6 | Local-first sync/replay and offline cache ownership hardening | WL-005, WL-011, WL-013 | in_progress | 2026-03-14 | Core browser persistence is complete (WL-011, WL-013); broader sync/replay hardening remains open under WL-005. |
| M7 | Play-safe Spider and coach surfaces integration | WL-006 | done | 2026-03-10 (actual) | Complete via tactical Spider card payloads and coach hints scoped by play role capabilities in mobile/tablet play surfaces. |
| M8 | Installable PWA hardening for mobile/tablet play | WL-007, WL-014 | done | 2026-03-10 (actual) | Complete: installability owns manifest/static shell assets, service-worker cache partition policy, quota/backpressure handling, bounded media-cache lifecycle pruning, and deep-link resume route ownership. |

Milestone coverage completeness:
- Milestone modeling coverage: complete.
- Milestone implementation completion: partial (M1-M5/M7/M8 complete, M6 in progress).

## Queue Publication Traceability

- 2026-03-11: advanced `WL-005` local-first runtime seam hardening by pruning unparseable browser-storage key residue during event-log/checkpoint/runtime-bundle reads and runtime-bundle quota accounting (`BrowserSessionEventLogStore`, `BrowserSessionOfflineCacheService`), with new regression coverage for unparseable key cleanup and quota integrity under malformed runtime-bundle key pressure in `src/Chummer.Play.RegressionChecks/Program.cs`; no duplicate queue/worklist row is appended.
- 2026-03-11: advanced `WL-005` local-first runtime seam hardening by dropping malformed persisted checkpoint/runtime-bundle/ledger records on read in `BrowserSessionOfflineCacheService` and `BrowserSessionEventLogStore`, with regression coverage in `src/Chummer.Play.RegressionChecks/Program.cs`; no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-163358-audit-task-48732.md` then `feedback/2026-03-11-163358-audit-task-2651.md` in oldest-first order; package-only boundary enforcement and contract-package naming remain implementation-closed through `WL-015`/`WL-016` (with package-feed cutover already done in `WL-017`), so no duplicate queue row is appended.
- 2026-03-11: advanced `WL-005` local-first runtime seam hardening by adding regression checks that prove browser-backed event-log continuity across service instances and runtime-bundle cache quota eviction/pressure behavior in `src/Chummer.Play.RegressionChecks/Program.cs`; no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-163358-audit-task-2650.md` and `feedback/2026-03-11-163358-audit-task-2653.md` in oldest-first order; package-only dependency boundaries and package-plane readiness remain implementation-closed through `WL-015`/`WL-016`/`WL-017` (milestones `M1` and `M4` done), milestone registry coverage remains complete through `M1`-`M8`, and stale generated queue prompts for those exact slices were removed from `.codex-studio/published/QUEUE.generated.yaml` to avoid duplicate backlog churn.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-163358-audit-task-4364.md` and `feedback/2026-03-11-163358-audit-task-11710.md` in oldest-first order; scaffold-placeholder closure remains implementation-backed through `WL-008`/`WL-009`/`WL-010`/`WL-011` with `WL-018`, repo-local `.codex-design/` mirror context remains present for review, and stale generated queue prompts were removed from `.codex-studio/published/QUEUE.generated.yaml` to avoid duplicating completed slices.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-163358-audit-task-11710.md`, `feedback/2026-03-11-163358-audit-task-2650.md`, `feedback/2026-03-11-163358-audit-task-2651.md`, `feedback/2026-03-11-163358-audit-task-2652.md`, `feedback/2026-03-11-163358-audit-task-2653.md`, `feedback/2026-03-11-163358-audit-task-4364.md`, `feedback/2026-03-11-163358-audit-task-48732.md`, `feedback/2026-03-11-163358-audit-task-48733.md`, `feedback/2026-03-11-163358-audit-task-48734.md`, `feedback/2026-03-11-163359-audit-task-4344.md`, `feedback/2026-03-11-163359-audit-task-4345.md`, `feedback/2026-03-11-163359-audit-task-48735.md`, `feedback/2026-03-11-163359-audit-task-48736.md`, `feedback/2026-03-11-163359-audit-task-48737.md`, and `feedback/2026-03-11-163359-audit-task-8675.md` in oldest-first order; package-plane dependency scope for future `Chummer.Play.Contracts`/`Chummer.Ui.Kit` remains implementation-closed through `WL-017` (`M4` done), so the stale queue prompts for that exact slice were removed from `.codex-studio/published/QUEUE.generated.yaml` and no duplicate backlog row was appended.
- 2026-03-11: system re-entry slice "`Resolved 2026-03-10`: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-2650.md`; package-only dependency boundary enforcement is already implementation-backed through `WL-015` and `WL-016` with verify guardrails in `scripts/ai/verify.sh`, while remaining package-plane feed cutover scope stays queued under `WL-017`." is now stale and fully closed; package-boundary enforcement remains implementation-closed through `WL-015`/`WL-016`, and package-plane feed cutover is implementation-closed through `WL-017` (`M4` done) with published-feed compatibility checks enforced in `scripts/ai/verify.sh`, so no duplicate queue/worklist row is appended.
- 2026-03-11: advanced `WL-005` local-first cache seam hardening by validating direct-caller checkpoint/runtime-bundle metadata in `BrowserSessionOfflineCacheService` (blank lineage fields and negative checkpoint sequence ownership now fail fast), with regression coverage added in `src/Chummer.Play.RegressionChecks/Program.cs`; no duplicate backlog row is appended.
- 2026-03-11: incorporated remaining unread auditor publications `feedback/2026-03-10-125626-audit-task-2650.md`, `feedback/2026-03-10-125626-audit-task-2651.md`, `feedback/2026-03-10-125626-audit-task-2652.md`, `feedback/2026-03-10-125626-audit-task-2653.md`, `feedback/2026-03-10-125626-audit-task-4344.md`, `feedback/2026-03-10-125626-audit-task-4345.md`, `feedback/2026-03-10-125626-audit-task-4364.md`, `feedback/2026-03-10-125626-audit-task-48732.md`, `feedback/2026-03-10-125626-audit-task-48733.md`, `feedback/2026-03-10-125626-audit-task-48734.md`, `feedback/2026-03-10-125626-audit-task-48735.md`, and `feedback/2026-03-10-125626-audit-task-48736.md` in oldest-first order; each maps to already materialized closure or active ownership (`WL-015`/`WL-016`, `WL-006`/`WL-005`, `WL-008`-`WL-014`, `WL-018`, and M1-M8 registry truth), so no duplicate queue/worklist row is appended.
- 2026-03-11: system re-entry slice "`Resolved 2026-03-10`: corrected stale queue/worklist narrative where WL-014 was previously described as queued; implementation is done while broader installability hardening remains queued under WL-007." is now stale and fully closed; `WL-014` and `WL-007` are both done (with milestone `M8` done), so no additional installability follow-up backlog row is required.
- 2026-03-11: system re-entry slice "`Resolved 2026-03-10`: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-48732.md`; package-only dependency boundary enforcement remains executable and implementation-backed by `WL-015` (done) with remaining feed cutover scope under `WL-017`." is now stale and fully closed; package-boundary enforcement remains implementation-backed by `WL-015`/`WL-016`, while feed cutover is already implementation-closed through `WL-017` (done, `M4` done) with published-feed compatibility checks enforced in `scripts/ai/verify.sh`, so no duplicate follow-up backlog row is required.
- 2026-03-11: incorporated remaining unread auditor publications `feedback/2026-03-11-090017-audit-task-11710.md`, `feedback/2026-03-11-090017-audit-task-2651.md`, `feedback/2026-03-11-090017-audit-task-2653.md`, `feedback/2026-03-11-090017-audit-task-48732.md`, and `feedback/2026-03-11-090018-audit-task-48736.md` (oldest-first across previously unlogged items); `.codex-design/` mirror sync remains present, package-boundary and naming scopes remain implementation-closed through `WL-015`/`WL-016`, milestone registry coverage remains complete through M1-M8, and scaffold-placeholder scope remains implementation-closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` with `WL-018`, so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publication `feedback/2026-03-11-090017-audit-task-48735.md` in oldest-first order; dedicated play API/offline cache/installable PWA scope remains implementation-closed through `WL-012` (done), `WL-013` (done), `WL-014` (done), and `WL-007` (done), so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publication `feedback/2026-03-11-090017-audit-task-4345.md` in oldest-first order after candidate 48735; scaffold-stage bootstrap/browser session client/event-log persistence scope remains implementation-closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` with `WL-018` regression guards, so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publication `feedback/2026-03-11-090017-audit-task-48734.md` (queue exhausted with uncovered scope) in oldest-first order; explicit player/GM role-shell split plus local-first sync ownership remain executable backlog-owned via `WL-006` (`M5`, done) and `WL-005` (`M6`, in_progress), so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publication `feedback/2026-03-11-090017-audit-task-4344.md`; dedicated play API/offline cache/installable PWA scope remains implementation-closed through `WL-012` (done), `WL-013` (done), `WL-014` (done), and `WL-007` (done), so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-090017-audit-task-48733.md` and `feedback/2026-03-11-090017-audit-task-2652.md` (oldest-first); contract package naming remains implementation-closed through `WL-016` (done), and explicit player/GM shell split plus local-first sync ownership remain executable and milestone-mapped through `WL-006` (`M5`, done) and `WL-005` (`M6`, in_progress), so no duplicate queue/worklist row is appended.
- 2026-03-11: incorporated unread auditor publications `feedback/2026-03-11-090017-audit-task-2650.md`, `feedback/2026-03-11-090017-audit-task-4364.md`, `feedback/2026-03-11-090018-audit-task-8675.md`, and `feedback/2026-03-11-090018-audit-task-48737.md`; package-only boundary enforcement remains implementation-closed through `WL-015`/`WL-016`, package-plane dependency cutover remains implementation-closed through `WL-017` (`M4` done), and scaffold-stage closure remains implementation-closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` with `WL-018`, so no duplicate queue/worklist item is appended.
- 2026-03-11: system re-entry slice "`Resolved 2026-03-10`: package-plane dependency finding for future `Chummer.Ui.Kit` and `Chummer.Play.Contracts` is runnable backlog-owned through `WL-017` (queued) and mapped to mobile milestone `M4` (in_progress) in `WORKLIST.md`; no duplicate queue item added while published feeds remain incomplete." is now fully closed; package-plane cutover and compatibility checks are already implementation-backed through `WL-017` (done) and mobile milestone `M4` (done), so no follow-up backlog row is needed.
- 2026-03-11: system re-entry slice "`Resolved 2026-03-10`: dedicated play API/offline cache/installable PWA uncovered scope is already runnable backlog-owned and implemented through WL-012 (done), WL-013 (done), and WL-014 (done), with remaining installability hardening tracked under WL-007." is now fully closed; the WL-012/WL-013/WL-014 closure remains correct and `WL-007` is also done, so no additional installability follow-up backlog row is needed.
- 2026-03-11: incorporated unread publication `feedback/2026-03-11-github-review-pr.md`; `/api/play/reconnect` now rejects stale lineage before any `GetOrCreateAsync` mutation (preventing ledger reset/event loss), quick-action/sync now map queue-level lineage drift `InvalidOperationException` paths to stale-safe responses, and regression coverage now includes endpoint-level reconnect conflict/no-mutation plus queue-exception stale conversion checks.
- 2026-03-11: incorporated unread publication `feedback/2026-03-11-github-review-pr.md`; reconnect-client stale conflicts now preserve recovery metadata via `PlayReconnectStaleException` on HTTP `409`, and `src/Chummer.Play.RegressionChecks/Chummer.Play.RegressionChecks.csproj` is now present and wired into `Chummer.Play.slnx` plus `scripts/ai/verify.sh` so regression coverage remains runnable on clean checkouts without adding duplicate backlog rows.
- 2026-03-10: completed `WL-017` package-plane cutover verification by extending `scripts/ai/verify.sh` published-feed compatibility checks to restore/build core/components/player/gm/web plus regression checks (and execute regression runtime) under both explicit `CHUMMER_PUBLISHED_FEED_SOURCES` and repo-local published-feed smoke fallback, closing restore-time compatibility coverage across the full play dependency surface.
- 2026-03-10: advanced `WL-005` local-first runtime seam ownership by introducing explicit `IPlayEventLogStore`, `IPlayOfflineCacheService`, and `IPlayOfflineQueueService` abstractions in `Chummer.Play.Core`; `Chummer.Play.Web` route handlers and runtime wiring now consume those interfaces so event-log persistence, runtime bundle cache, and offline sync/replay queue behavior remain decoupled from browser implementation details.
- 2026-03-10: incorporated unread lead-dev publication `feedback/2026-03-10-lead-dev-external-tools-plane-feedback.md`; `chummer-play` remains client-side projection/ledger-only with no vendor keys, vendor SDK ownership, or direct provider orchestration added, so no duplicate backlog row is appended.
- 2026-03-10: incorporated unread publication `feedback/2026-03-10-github-review-pr.md`; stale quick-action/sync rejection now rebuilds projections from stored-ledger pending events and `LastKnownSequence`, with regression coverage for the checkpoint-missing path in `src/Chummer.Play.RegressionChecks/Program.cs`, so the review finding is implementation-closed without duplicating backlog rows.
- 2026-03-10: incorporated unread publication `feedback/2026-03-10-github-review-pr.md`; high/medium findings are now implementation-backed in `BrowserSessionEventLogStore` lineage reset, `Program.cs` full stored-lineage stale rejection for quick-action/sync, monotonic enqueue sequence ownership in `BrowserSessionOfflineQueueService`, and regression automation coverage wired through `src/Chummer.Play.RegressionChecks` plus `scripts/ai/verify.sh`.
- 2026-03-10: incorporated unread publication `feedback/2026-03-10-public-repo-graph-audit.md`; canonical package ids already remain frozen in README/build props as `Chummer.Engine.Contracts`, `Chummer.Play.Contracts`, and `Chummer.Ui.Kit`, and repo-local mirror visibility was tightened in README plus `docs/chummer-play.design.v1.md` was refreshed to reflect the real play API/ledger/sync seam instead of stale scaffold-state text, so no duplicate backlog row is appended.
- 2026-03-10: queue request "Add milestone mapping or executable queue work for Contract package naming is still inconsistent between the play repo docs and package props." remains closed through `WL-016` (done) with package/property alignment guardrails in `scripts/ai/verify.sh`, so no duplicate queue item is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-2652.md`; player and GM role-shell split plus local-first sync ownership remain executable and milestone-mapped via `WL-006` (done, `M5`) and `WL-005` (queued, `M6`), so no duplicate queue row is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-48734.md`; queue-exhaustion coverage for role-shell and local-first sync scope remains materially owned by `WL-006` and `WL-005`, preserving runnable backlog without reopening completed role-shell work.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-48736.md`; bootstrap/session-client/event-log placeholder scope remains executable backlog-owned and implementation-complete through `WL-008`/`WL-009`/`WL-010`/`WL-011`, with scaffold-regression coverage enforced by `WL-018`, so no duplicate queue item is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-8675.md`; future `Chummer.Play.Contracts` + `Chummer.Ui.Kit` package-plane dependency scope remains explicitly materialized via `WL-017` (queued) under milestone `M4` (in_progress), so milestone mapping remains executable without re-queuing completed work.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-48735.md` (candidate 48735, queue exhausted with uncovered scope); dedicated play API/offline cache/installable PWA scope remains executable backlog-owned via `WL-012` (done), `WL-013` (done), and `WL-014` (done), mapped to M4/M6/M8 while broader M8 installability hardening remains queued under `WL-007`.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-4345.md` (candidate 4345, uncovered scaffold scope); bootstrap/browser session client/event-log persistence scaffold-state closure remains implementation-complete via `WL-008`/`WL-009`/`WL-010`/`WL-011` with `WL-018` regression guards, so no duplicate re-queue is added.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-48734.md` (candidate 48734, queue exhausted with uncovered scope); explicit player/GM role-shell split plus local-first sync ownership remains runnable backlog through `WL-006` (M5, done) and `WL-005` (M6, in_progress), so no duplicate implementation item is added.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-4344.md` (candidate 4344, uncovered scope); dedicated play API/offline cache/installable PWA ownership remains materialized through `WL-012` (done), `WL-013` (done), and `WL-014` (done) with milestone mapping in M4/M6/M8.
- 2026-03-10: unread `2026-03-10-082709` auditor findings for package-boundary enforcement (candidate 2650) and milestone coverage completeness (candidate 2653) are incorporated and closed in-repo: package enforcement remains implementation-backed through `WL-015`/`WL-016` plus `scripts/ai/verify.sh`, and milestone modeling remains complete through the M1-M8 registry with explicit ETA/completion truth.
- 2026-03-10: uncovered-scope candidate 2652 and queue-exhaustion candidate 48734 from the unread `2026-03-10-082709` auditor publication set remain explicitly materialized as runnable backlog through `WL-006` (player/GM role shell split and gating, done, M5) and `WL-005` (local-first sync/runtime seam hardening, queued, M6).
- 2026-03-10: queue-exhaustion finding candidate 48734 (player/GM role shells plus local-first sync ownership) is already materialized as runnable backlog via `WL-006` (role shell split/gating, done) and `WL-005` (local-first sync/runtime seam hardening, queued/in_progress milestone coverage in M6). Source audit publication: `feedback/2026-03-10-065227-audit-task-48734.md`.
- 2026-03-10: implemented `WL-006` closure for role-shell split and tactical play surfaces; bootstrap now emits only the active role shell, role-scoped capabilities, Spider cards, coach hints, and stale-protected `/api/play/quick-action` payloads without exposing GM-only actions to player flows.
- 2026-03-10: queue-exhaustion finding candidate 48737 (future `Chummer.Play.Contracts`/`Chummer.Ui.Kit` package-plane readiness) remains explicitly backlog-owned through `WL-017` (queued, M4 in progress) with restore-time feed compatibility checks and package-only fallback constraints until published feeds are fully real. Source audit publication: `feedback/2026-03-10-065227-audit-task-48737.md`.
- 2026-03-10: queue-exhaustion finding candidate 48736 (bootstrap/session-client/event-log scaffold placeholders) is already materialized and completed via `WL-008`/`WL-009`/`WL-010`/`WL-011`, with regression closure enforced by `WL-018` verify guards. Source audit publication: `feedback/2026-03-10-065227-audit-task-48736.md`.
- 2026-03-10: unread `2026-03-10-082709` scaffold-state findings (candidates 4345 and 4364) remain closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` plus `WL-018` regression guards, so no duplicate scaffold work is re-queued.
- 2026-03-10: dedicated play API, offline cache, and installable PWA uncovered-scope findings (feedback candidates 4344 and 48735) are materialized as runnable backlog through `WL-012` (done), `WL-013` (done), and `WL-014` (done), with remaining M8 installability hardening tracked under `WL-007`.
- 2026-03-10: re-entry validation for feedback candidates 4345 and 48735 confirms scaffold placeholders are already closed in executable code (`Program.cs`, `BrowserSessionApiClient`, `BrowserSessionEventLogStore`) and queue ownership remains explicit through `WL-008`/`WL-009`/`WL-010`/`WL-011`/`WL-018` plus `WL-012`/`WL-013`/`WL-014`; no duplicate worklist item added.
- 2026-03-10: implemented `WL-014` closure for installable play-shell hardening; `Chummer.Play.Web` now ships `manifest.webmanifest`, `service-worker.js`, bounded runtime-bundle cache quota/backpressure eviction in `BrowserSessionOfflineCacheService`, and explicit deep-link resume ownership through `/api/play/resume/{sessionId}` and `/play/{sessionId}`.
- 2026-03-10: package-boundary uncovered-scope findings (feedback candidates 2650/48732 and 2651/48733) remain explicitly materialized through `WL-015` (done) and `WL-016` (done), preserving executable ownership and queue traceability.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-082709-audit-task-48732.md` and `feedback/2026-03-10-082709-audit-task-2651.md`; package-boundary enforcement and package-naming scope remain executable backlog-owned via `WL-015` (done), `WL-016` (done), and `WL-017` (queued for package-feed cutover).
- 2026-03-10: contract package naming inconsistency between play docs and package props is now closed via `WL-016` with concrete updates in `Directory.Build.props`, `Directory.Packages.props`, consuming `*.csproj`, and `scripts/ai/verify.sh`.
- 2026-03-10: unread `2026-03-10-082709` milestone-coverage finding candidate 2653 is closed in this repo mirror via the Mobile Milestone Registry completion notes (`Milestone modeling coverage: complete`) and explicit M1-M8 ETA/completion truth mapping above.
- 2026-03-10: package-plane queue item "The play repo still depends on the future `Chummer.Ui.Kit` and `Chummer.Play.Contracts` package plane that is not yet fully real" is explicitly materialized as runnable backlog through `WL-017` (queued) under milestone `M4` (in_progress), with restore-time compatibility checks and package-only fallback constraints until published feeds are fully real.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-101409-audit-task-8675.md` and `feedback/2026-03-10-101409-audit-task-48737.md`; future package-plane dependency scope remains explicitly milestone-mapped and backlog-owned by `WL-017` under `M4` until published `Chummer.Play.Contracts` and `Chummer.Ui.Kit` feeds are fully real.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-082709-audit-task-48737.md`; queue-exhaustion scope for future `Chummer.Ui.Kit` and `Chummer.Play.Contracts` package-plane readiness remains executable backlog-owned by `WL-017` under mobile milestone `M4` (in_progress), so no duplicate queue row is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-4364.md`; scaffold-stage bootstrap/session-client/event-log finding remains closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` plus `WL-018` regression guards, and dedicated play API/offline cache/PWA ownership remains materialized via `WL-012`/`WL-013`/`WL-014` mapped in milestones `M4`/`M6`/`M8`.
- 2026-03-10: queue request "Add milestone mapping or executable queue work for Dedicated play API, offline cache, and installable PWA hardening work remains unmaterialized" is resolved as already materialized backlog and implementation ownership through `WL-012` (done), `WL-013` (done), and `WL-014` (done), with remaining installability umbrella scope still tracked under `WL-007`.
- 2026-03-10: incorporated slice "The mobile and play-mode shell split still needs explicit player and GM role shells plus local-first sync ownership"; executable ownership remains explicit with role-shell separation closed under `WL-006` (done, `M5`) and remaining local-first sync/replay hardening owned by `WL-005` (`M6` in_progress), so no duplicate queue row is appended.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-101409-audit-task-2650.md` and `feedback/2026-03-10-101409-audit-task-2653.md`; package-only dependency boundary enforcement remains implementation-backed by `WL-015` and milestone coverage modeling remains complete in the M1-M8 registry (ETA and completion truth present for each milestone), so no duplicate queue row is appended.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-101409-audit-task-2651.md` and `feedback/2026-03-10-101409-audit-task-48733.md`; contract package naming inconsistency remains implementation-closed through `WL-016` with package/property consistency guarded by `scripts/ai/verify.sh`, while `M4` keeps remaining package-feed cutover work under `WL-017`.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-48732.md`; queue-exhaustion package-boundary scope remains closed via `WL-015` (done) and naming alignment via `WL-016` (done), with no re-queue required.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-101409-audit-task-4344.md` and `feedback/2026-03-10-101409-audit-task-48735.md`; dedicated play API/offline cache/installable PWA uncovered scope remains materially closed through `WL-012` (done), `WL-013` (done), and `WL-014` (done), with remaining installability umbrella work still queued under `WL-007`.
- 2026-03-10: system re-entry validation for milestone coverage found queue drift; mobile milestone registry modeling remains complete (M1-M8 include explicit ETA and completion truth), so stale queue prompt "Finish milestone coverage modeling for mobile so ETA and completion truth are no longer partial." was removed from `.codex-studio/published/QUEUE.generated.yaml` instead of duplicating backlog.
- 2026-03-10: queue request "Publish or append runnable backlog for Package-only dependency boundaries for `Chummer.Engine.Contracts`, `Chummer.Play.Contracts`, and `Chummer.Ui.Kit` are not yet enforced in code." remains implementation-closed via `WL-015` (done) with package/property hardening in `WL-016` (done), while package-feed cutover remains explicit runnable backlog under `WL-017` (queued, milestone `M4` in_progress), so no duplicate queue item is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-4345.md`; uncovered scaffold-stage bootstrap/browser session client/event-log persistence scope remains implementation-closed through `WL-008`/`WL-009`/`WL-010`/`WL-011` with regression protection in `WL-018`, so no duplicate backlog row is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-101409-audit-task-48736.md`; queue-exhaustion finding for the same scaffold-placeholder scope remains materialized and closed with milestone mapping in `M3` (done, actual 2026-03-10), so no queue append is required.
- 2026-03-10: incorporated older unread auditor publications `feedback/2026-03-09-125540-audit-task-4364.md`, `feedback/2026-03-09-155355-audit-task-2650.md`, `feedback/2026-03-09-155355-audit-task-2651.md`, `feedback/2026-03-09-155355-audit-task-2652.md`, `feedback/2026-03-09-155355-audit-task-4344.md`, `feedback/2026-03-09-155355-audit-task-4345.md`, `feedback/2026-03-09-185355-audit-task-2653.md`, `feedback/2026-03-09-185355-audit-task-8675.md`, `feedback/2026-03-09-mobile-play-split-guide.md`, `feedback/2026-03-10-065227-audit-task-48732.md`, `feedback/2026-03-10-065227-audit-task-48733.md`, `feedback/2026-03-10-065227-audit-task-48735.md`, and `feedback/2026-03-10-082709-audit-task-48733.md`; all map to already materialized/closed scope (`WL-006`, `WL-008`-`WL-018`), so no duplicate implementation rows are appended.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-082709-audit-task-2650.md`, `feedback/2026-03-10-082709-audit-task-2652.md`, `feedback/2026-03-10-082709-audit-task-2653.md`, and `feedback/2026-03-10-082709-audit-task-4364.md`; each remains implementation-closed or milestone-mapped in existing backlog (`WL-015`/`WL-016`, `WL-006`/`WL-005`, M1-M8 registry coverage, and `WL-008`/`WL-009`/`WL-010`/`WL-011` + `WL-018`), so no duplicate queue row is appended.
- 2026-03-10: incorporated previously unreferenced feedback publication `feedback/2026-03-09-change-plan-mobile.md`; requested package-name normalization, real `Chummer.Play.Contracts`/`Chummer.Ui.Kit` package-plane readiness, scaffold replacement, and in-repo player/GM shell ownership remain explicitly mapped to existing backlog (`WL-016` done, `WL-017` queued, `WL-008` through `WL-011` done, `WL-006` done), so no duplicate queue row is appended.
- 2026-03-10: incorporated unread auditor publication `feedback/2026-03-10-125625-audit-task-11710.md`; repo-local design mirror files and review context are present under `.codex-design/` with 2026-03-10 refresh timestamps, so no duplicate mirror-refresh backlog row is appended.
- 2026-03-10: incorporated unread auditor publications `feedback/2026-03-10-125626-audit-task-8675.md` and `feedback/2026-03-10-125626-audit-task-48737.md`; the package-plane dependency slice for future `Chummer.Play.Contracts`/`Chummer.Ui.Kit` remains explicit runnable backlog via `WL-017` (`M4` in progress), so no duplicate queue item is appended.

- 2026-03-10: incorporated unread auditor publication set `feedback/2026-03-10-125626-audit-task-{2650,2651,2652,2653,4344,4345,4364,48732,48733,48734,48735,48736}.md`; these findings remain mapped to existing executable backlog and milestone coverage (`WL-005`/`WL-006`, `WL-008`-`WL-014`, `WL-015`/`WL-016`, and M1-M8 registry), so no duplicate queue rows are appended.
- 2026-03-10: system re-entry slice "Publish or append runnable backlog for Contract package naming is still inconsistent between the play repo docs and package props." remains implementation-closed via `WL-016` (done) with naming guardrails enforced in `scripts/ai/verify.sh`, while remaining package-feed cutover stays explicitly backlog-owned by `WL-017` (`M4` in_progress), so no duplicate queue row is appended.
- 2026-03-10: system re-entry slice "Add milestone mapping or executable queue work for Bootstrap, browser session client, and event-log persistence code are still scaffold-stage placeholders." remains implementation-closed and milestone-mapped through `WL-008`/`WL-009`/`WL-010`/`WL-011` + `WL-018` (`M3` done, actual 2026-03-10), so no duplicate worklist row is appended.
- 2026-03-10: system re-entry slice "Publish or append runnable backlog for Dedicated play API, offline cache, and installable PWA hardening work remains unmaterialized." remains implementation-closed and milestone-mapped through `WL-012`/`WL-013`/`WL-014` (`M4`/`M6`/`M8`), with remaining installability umbrella hardening still tracked under `WL-007`, so no duplicate worklist row is appended.
- 2026-03-10: system re-entry slice "Add milestone mapping or executable queue work for The mobile and play-mode shell split still needs explicit player and GM role shells plus local-first sync ownership." remains explicitly mapped to `WL-006` (`M5`, done) and `WL-005` (`M6`, in_progress); verification guardrails were tightened in `scripts/ai/verify.sh` to enforce role-shell separation and `BrowserSessionOfflineQueueService` sync-ownership seams so this slice stays executable without re-queuing duplicate work.
- 2026-03-10: system re-entry slice "Add milestone mapping or executable queue work for Contract package naming is still inconsistent between the play repo docs and package props." remains milestone-mapped and implementation-closed via `WL-016` (done) under `M4`; stale unresolved contract-naming prompts were removed from `.codex-studio/published/QUEUE.generated.yaml` so queue publication reflects completed work while `WL-017` remains the only open package-plane follow-up.
- 2026-03-10: system re-entry slice "Publish or append runnable backlog for Package-only dependency boundaries for `Chummer.Engine.Contracts`, `Chummer.Play.Contracts`, and `Chummer.Ui.Kit` are not yet enforced in code." remains implementation-closed through `WL-015` (done) and `WL-016` (done), with package-feed cutover still explicitly tracked by `WL-017` (`M4` in_progress); stale unresolved package-boundary prompts were removed from `.codex-studio/published/QUEUE.generated.yaml` to prevent duplicate backlog churn.
- 2026-03-10: system re-entry slice "Move the play repo from placeholder bootstrap/client/storage code onto the real play API, browser persistence, and sync seams." is implementation-closed and milestone-mapped via `WL-008`/`WL-009`/`WL-010`/`WL-011` (`M3` done) plus dedicated route/cache ownership in `WL-012`/`WL-013` and sync hardening under `WL-005`; stale unresolved queue prompt was removed from `.codex-studio/published/QUEUE.generated.yaml` to prevent duplicate backlog churn.
- 2026-03-10: system re-entry slice "Finish milestone coverage modeling for mobile so ETA and completion truth are no longer partial." is closed: M1-M8 ETA/completion truth remains fully modeled in the Mobile Milestone Registry, `.codex-studio/published/QUEUE.generated.yaml` no longer carries the unresolved raw slice line, and summary completion text now matches `M8` as done.

## Intake Template
Add new items at the bottom:

`| WL-XXX | queued | P1/P2/P3 | <task> | agent/user | <constraints, links, commands> |`
